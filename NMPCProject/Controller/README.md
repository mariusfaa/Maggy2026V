This folder should be copied into MagLevTbx folder. Pathing in scripts need to be modified for your system.

After having set up the toolbox, build and deploy TeensyNMPC_IO.slx, then run hardwareNMPCController.m (in this script you also need to specify port)