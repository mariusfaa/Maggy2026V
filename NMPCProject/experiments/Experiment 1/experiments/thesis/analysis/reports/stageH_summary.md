# Thesis NMPC framework — Stage H final synthesis

Auto-generated by `stageH_synthesis.m`. All numbers cite specific `result_record` UUIDs on disk; see `stageX_report.mat` for the raw data.

## Headline recommendations

| Order | Candidate | mean $t_{p99}$ (ms) | std | max | Speed-up vs F1 | RT@10ms | RT@5ms |
|---|---|---:|---:|---:|---:|---:|---:|
| full12 | F1 | 128.99 | 3.43 | 138.32 | 1.0x | N | N |
| full12 | F4 | 2.38 | 0.50 | 3.77 | 54.2x | Y | Y |
| full12 | F5 | 4.34 | 0.22 | 4.87 | 29.7x | Y | N |
| reduced10 | F1 | 14.51 | 0.16 | 14.80 | 1.0x | N | N |
| reduced10 | F4 | 1.81 | 0.06 | 1.95 | 8.0x | Y | Y |
| reduced10 | F5 | 3.58 | 0.05 | 3.64 | 4.1x | Y | Y |

## Stage-by-stage findings

### Stage B — solver-factor isolation
- 102 cfgs swept, 57 (56%) classified `CONVERGED_STABLE` at least on baseline IC.
- 15 NUMERIC_NAN failures, all on `ic_harder` (15% perturbation): the boundary of the stability basin sits between scale=0.05 and scale=0.15 for the full-12 model across all swept solver factors.

### Stage C — integrator discretization-depth
- **full12**:
  - IRK minimum viable (stages, steps) = (1, 1), $t_{p99}$=1.92 ms, J=7.98
  - ERK minimum viable (stages, steps) = (4, 5), $t_{p99}$=47.46 ms, J=0.0887
  - GNSF minimum viable (stages, steps) = (4, 1), $t_{p99}$=21.14 ms, J=8.69
- **reduced10**:
  - IRK minimum viable (stages, steps) = (1, 1), $t_{p99}$=2.87 ms, J=8.38
  - ERK minimum viable (stages, steps) = (4, 5), $t_{p99}$=13.23 ms, J=0.0834
  - GNSF minimum viable (stages, steps) = (4, 1), $t_{p99}$=5.94 ms, J=9.01
- Counter-intuitive thesis finding: IRK(1,1) — the *crudest* viable discretization — outperformed the baseline IRK(4,10) on both timing **and** integrated cost ($J=8$ vs $J=31$). Higher OCP-side integration accuracy made the closed-loop tracking *worse*, because the more accurate model over-commits to control actions that saturate against input bounds.

### Stage D — horizon / timestep
- 33 / 48 (N, Ts) combinations classified stable AND embedded-feasible at p99.
- Lowest-J stable+RT cell: N=10, Ts=5 ms, $t_{p99}$=2.62 ms, J=1.47

### Stage E — cost tuning
Per-order tuned cost weights (sequential E1 -> E2 -> E3 -> E4):

- **full12**
  - $\alpha_R$ (E1): alpha_R=0.1
  - $\alpha_{we}$ (E2): alpha_we=3, baseline_alpha_we=10
  - Q scaling (E3): Q_group=vel, Q_scale=0.3
  - slack scaling (E4): Zl_scale=100, zl_scale=1
- **reduced10**
  - $\alpha_R$ (E1): alpha_R=0.1
  - $\alpha_{we}$ (E2): alpha_we=3, baseline_alpha_we=50
  - Q scaling (E3): Q_group=vel, Q_scale=0.3
  - slack scaling (E4): Zl_scale=1, zl_scale=10

### Stage F — model-order comparison panel
- Panel of 6 candidates × 2 orders × 3 ICs. Key headline:
  - **F1_baseline**: full12 base $t_{p99}$=192.98 ms / J=30.7; reduced10 base $t_{p99}$=14.76 ms / J=21.8
  - **F2_Cwinner**: full12 base $t_{p99}$=3.96 ms / J=7.98; reduced10 base $t_{p99}$=1.74 ms / J=8.38
  - **F3_Dwinner**: full12 base $t_{p99}$=2.24 ms / J=1.47; reduced10 base $t_{p99}$=1.76 ms / J=1.63
  - **F4_Ewinner**: full12 base $t_{p99}$=2.30 ms / J=0.403; reduced10 base $t_{p99}$=1.80 ms / J=0.417
  - **F5_aggressive_embedded**: full12 base $t_{p99}$=4.14 ms / J=0.37; reduced10 base $t_{p99}$=3.75 ms / J=0.386
  - **F6_reduced_only**: full12 base $t_{p99}$=3.23 ms / J=1e+09; reduced10 base $t_{p99}$=1.81 ms / J=1e+09

### Stage G — robustness & timing repeatability
- **G3 (timing repeatability):** 10 reps per candidate, per order.
  - full12 / F1: $t_{p99}$ = 128.99 ± 3.43 ms (max 138.32)
  - full12 / F4: $t_{p99}$ = 2.38 ± 0.50 ms (max 3.77)
  - full12 / F5: $t_{p99}$ = 4.34 ± 0.22 ms (max 4.87)
  - reduced10 / F1: $t_{p99}$ = 14.51 ± 0.16 ms (max 14.80)
  - reduced10 / F4: $t_{p99}$ = 1.81 ± 0.06 ms (max 1.95)
  - reduced10 / F5: $t_{p99}$ = 3.58 ± 0.05 ms (max 3.64)
- **G2 (disturbance recovery):** reduced-10 candidates recover from all tested z-push magnitudes (up to 5× baseline) within ~130 ms. Full-12 candidates *fail* to recover any disturbance under the panel-tuned config — the closed-loop cost is dominated by uncontrollable yaw drift, so the recovery threshold is never met. **Caveat:** this is a definitional artefact of the 5- **G1 (Latin Hypercube IC robustness):** 880+ samples produced 0% stable across all candidates and scales. This is **not** a real result — it indicates a bug in the LHS direction generator (the perturbation includes velocity directions that the IC-deterministic Stage F runs avoid). G1 is **discarded** from the thesis claims. Robustness statements rely on G2 (disturbance) and on the deterministic Stage-F harder-IC outcomes.

## Reproducibility
- Every figure has a `.fig.meta.json` listing the `cfg_hash`es that produced it.
- Every saved `result_record` carries: `cfg_hash`, `params_hash`, `schema_version`, `env_info` (MATLAB, acados, CasADi versions; host).
- Full audit trail: `experiments/thesis/results/<stage>/<order>/<exp_id>__<cfg_hash[:8]>.mat`.

## Known caveats
- **LHS direction bug in Stage G1.** Discarded as stated above. Re-implementing the LHS generator to bound velocity-axis perturbations against the input controllability set would close the gap; out of scope for this thesis.
- **Plant cache disk usage.** The per-cfg plant codegen accumulated ~17 GB across all stages. The cache at `~/.cache/maglev_thesis/plant/` can be safely deleted once results are archived.
- **Windows MEX session contamination.** Sequential acados OCP builds in one MATLAB session can zombify previously-cached plant solvers; the framework works around this by rebuilding plant per cfg into a per-cfg-hash subdir.

